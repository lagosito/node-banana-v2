import CampaignGenerator from "./CampaignGenerator";

export const metadata = {
  title: "Campaign Generator — El Kiosk",
  description: "Generate complete marketing campaigns with AI — images, videos, captions, and hashtags in one click.",
};

export default function CampaignPage() {
  return <CampaignGenerator />;
}
